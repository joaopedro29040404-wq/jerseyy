'use client';
import {useState} from 'react';
export default function Header({logo,instagram,storeName}:{logo:string|null;instagram:string;storeName:string}){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-white/10 bg-black/90 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><a href="/" className="flex items-center gap-3">{logo?<img src={logo} className="h-9 w-9 object-contain" alt="Jersey Gold"/>:<span className="text-2xl font-black gold">JG</span>}<div><div className="font-black tracking-[.18em]">{storeName}</div><div className="text-[9px] tracking-[.35em] text-white/50">FOOTBALL STORE</div></div></a><button onClick={()=>setOpen(!open)} className="rounded border border-white/15 px-3 py-2 text-sm md:hidden">MENU</button><nav className={`${open?'flex':'hidden'} absolute left-0 right-0 top-full flex-col gap-4 border-b border-white/10 bg-black p-5 md:static md:flex md:flex-row md:border-0 md:bg-transparent`}><a href="#categorias" onClick={()=>setOpen(false)}>Categorias</a><a href="#catalogo" onClick={()=>setOpen(false)}>Catálogo</a>{instagram&&<a href={instagram.startsWith('http')?instagram:`https://instagram.com/${instagram.replace('@','')}`} target="_blank">Instagram</a>}<a href="/admin">Admin</a></nav></div></header>}
