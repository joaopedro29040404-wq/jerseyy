'use client';
export default function Categories({categories}:{categories:any[]}){return <section id="categorias" className="mx-auto max-w-7xl px-5 py-14"><div className="mb-8"><p className="text-xs font-bold tracking-[.3em] gold">EXPLORE</p><h2 className="mt-2 text-3xl font-black">CATEGORIAS</h2></div><div className="grid grid-cols-1 gap-4 sm:grid-cols-3">{categories.map(c=><button key={c.id} onClick={()=>{window.dispatchEvent(new CustomEvent('jersey-category',{detail:c.id}));document.getElementById('catalogo')?.scrollIntoView()}} className="group overflow-hidden rounded-2xl border border-white/10 bg-white/[.03] text-left"><div className="aspect-[16/10] bg-black">{c.image_url?<img src={c.image_url} alt={c.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105"/>:<img src="/demo/category-placeholder.svg" alt="Demonstração" className="h-full w-full object-cover"/>}</div><div className="p-5"><div className="text-xl font-black">{c.name}</div><div className="mt-1 text-sm text-white/50">{c.product_count??0} produto(s)</div></div></button>)}</div></section>}
