'use client';
import {useEffect,useMemo,useState} from 'react'; import {money} from '@/lib/format';
export default function Catalog({products,categories}:{products:any[];categories:any[]}){const [q,setQ]=useState(''),[cat,setCat]=useState(''); useEffect(()=>{const f=(e:any)=>setCat(e.detail); window.addEventListener('jersey-category',f); return()=>window.removeEventListener('jersey-category',f)},[]); const [season,setSeason]=useState(''),[price,setPrice]=useState(''),[sort,setSort]=useState('recent'); const seasons=[...new Set(products.map(p=>p.season))]; const list=useMemo(()=>products.filter(p=>(!q||`${p.name} ${p.team} ${p.season}`.toLowerCase().includes(q.toLowerCase()))&&(!cat||p.category_id===cat)&&(!season||p.season===season)&&(!price||(price==='low'&&p.price<200)||(price==='mid'&&p.price>=200&&p.price<300)||(price==='high'&&p.price>=300))).sort((a,b)=>sort==='az'?a.name.localeCompare(b.name):sort==='low'?a.price-b.price:sort==='high'?b.price-a.price:new Date(b.created_at).getTime()-new Date(a.created_at).getTime()),[products,q,cat,season,price,sort]);return <section id="catalogo" className="mx-auto max-w-7xl px-5 py-14"><div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-xs font-bold tracking-[.3em] gold">JERSEY GOLD</p><h2 className="mt-2 text-3xl font-black">CATÁLOGO</h2></div><div className="text-sm text-white/50">{list.length} resultado(s)</div></div><div className="grid gap-3 md:grid-cols-5"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar nome, time..." className="rounded border border-white/10 bg-white/5 px-4 py-3 outline-none focus:border-[#d4af37] md:col-span-2"/><select value={cat} onChange={e=>setCat(e.target.value)} className="rounded border border-white/10 bg-[#101010] px-3 py-3"><option value="">Categoria</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><select value={season} onChange={e=>setSeason(e.target.value)} className="rounded border border-white/10 bg-[#101010] px-3 py-3"><option value="">Temporada</option>{seasons.map(s=><option key={s}>{s}</option>)}</select><select value={sort} onChange={e=>setSort(e.target.value)} className="rounded border border-white/10 bg-[#101010] px-3 py-3"><option value="recent">Mais recentes</option><option value="low">Menor preço</option><option value="high">Maior preço</option><option value="az">A-Z</option></select></div><div className="mt-3"><select value={price} onChange={e=>setPrice(e.target.value)} className="rounded border border-white/10 bg-[#101010] px-3 py-3"><option value="">Faixa de preço</option><option value="low">Até R$ 199,99</option><option value="mid">R$ 200 a R$ 299,99</option><option value="high">R$ 300+</option></select></div><div className="mt-8 grid grid-cols-2 gap-4 md:grid-cols-3 lg:grid-cols-4">{list.map(p=><a href={`/produto/${p.id}`} key={p.id} className="group overflow-hidden rounded-2xl border border-white/10 bg-white/[.03]"><div className="aspect-[4/5] bg-black">{p.images?.[0]?<img src={p.images[0]} alt={p.name} className="h-full w-full object-cover transition group-hover:scale-105"/>:<img src="/demo/jersey-placeholder.svg" alt="Imagem de demonstração" className="h-full w-full object-cover"/>}</div><div className="p-4"><div className="text-sm text-white/50">{p.category?.name} · {p.season}</div><h3 className="mt-1 font-black">{p.name}</h3><div className="mt-3 gold font-black">{money(p.price)}</div></div></a>)}</div></section>}
