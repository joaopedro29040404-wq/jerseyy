import {redirect} from 'next/navigation'; import {createClient} from '@/lib/supabase-server';
export default async function Admin(){const s=await createClient(); const {data:{user}}=await s.auth.getUser(); if(!user) redirect('/admin/login'); if(user.app_metadata?.role!=='admin') return <main className="min-h-screen p-8"><h1 className="text-2xl font-black">Acesso não autorizado</h1><p className="mt-2 text-white/60">O usuário autenticado não possui a role admin.</p></main>; const [{count:products},{count:categories},{count:sizes},{data:stock}]=await Promise.all([s.from('products').select('*',{count:'exact',head:true}),s.from('categories').select('*',{count:'exact',head:true}),s.from('sizes').select('*',{count:'exact',head:true}),s.from('product_size_stock').select('stock')]); const out=(stock??[]).filter((x:any)=>x.stock<=0).length; return <main className="min-h-screen bg-[#050505] p-5 md:p-10"><div className="mx-auto max-w-6xl"><div className="flex justify-between"><div><div className="gold text-xs tracking-[.3em]">JERSEY GOLD</div><h1 className="mt-2 text-4xl font-black">ADMIN</h1></div><form action="/admin/logout" method="post"><button className="border border-white/10 px-4 py-2">Sair</button></form></div><div className="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[['Produtos',products],['Categorias',categories],['Tamanhos',sizes],['Sem estoque',out]].map(([n,v])=><div key={String(n)} className="rounded-2xl border border-white/10 p-6"><div className="text-white/50">{n}</div><div className="mt-2 text-4xl font-black">{v??0}</div></div>)}</div><nav className="mt-10 grid gap-3 sm:grid-cols-2"><a className="rounded border border-white/10 p-5" href="/admin/produtos">Produtos →</a><a className="rounded border border-white/10 p-5" href="/admin/categorias">Categorias →</a><a className="rounded border border-white/10 p-5" href="/admin/tamanhos">Tamanhos →</a><a className="rounded border border-white/10 p-5" href="/admin/configuracoes">Configurações →</a></nav></div></main>}
