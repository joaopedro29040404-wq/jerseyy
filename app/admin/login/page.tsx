'use client'; import {useState} from 'react'; import {createClient} from '@/lib/supabase-browser'; import {useRouter} from 'next/navigation';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''); const r=useRouter(); async function submit(e:any){e.preventDefault();setError('');const {error}=await createClient().auth.signInWithPassword({email,password}); if(error)setError(error.message); else r.push('/admin');} return <main className="flex min-h-screen items-center justify-center bg-[#050505] p-5"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border border-white/10 bg-white/[.03] p-7"><div className="gold text-xs font-bold tracking-[.3em]">JERSEY GOLD</div><h1 className="mt-2 text-3xl font-black">Acesso administrativo</h1><input required type="email" placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)} className="mt-8 w-full rounded border border-white/10 bg-black px-4 py-3"/><input required type="password" placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)} className="mt-3 w-full rounded border border-white/10 bg-black px-4 py-3"/><button className="mt-5 w-full bg-gold px-4 py-3 font-black text-black">ENTRAR</button>{error&&<p className="mt-4 text-sm text-red-400">{error}</p>}</form></main>}
