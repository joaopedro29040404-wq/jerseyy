'use client';
import {FormEvent,useState} from 'react';import {createClient} from '@/lib/supabase-browser';import {useRouter} from 'next/navigation';
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(loading)return;setError('');setLoading(true);try{const s=createClient();const{error}=await s.auth.signInWithPassword({email:email.trim(),password});if(error){setError(error.message);return}await s.auth.refreshSession();router.replace('/admin');router.refresh()}catch(err){setError(err instanceof Error?err.message:'Não foi possível entrar.')}finally{setLoading(false)}}return <main className="flex min-h-screen items-center justify-center bg-[#030303] p-5"><div className="absolute inset-0 bg-[radial-gradient(circle_at_50%_35%,rgba(212,175,55,.10),transparent_35%)]"/><form onSubmit={submit} className="relative w-full max-w-md rounded-xl border border-[#8e711d]/70 bg-[#080808] p-8 shadow-[0_25px_80px_rgba(0,0,0,.6)]"><div className="mx-auto mb-5 h-24 w-24 overflow-hidden rounded-lg border border-[#d4af37]/70 bg-black p-1"><img src="/demo/logo.svg" alt="Jersey Gold" className="h-full w-full object-contain"/></div><div className="text-center"><div className="text-[10px] font-black tracking-[.35em] gold">JERSEY GOLD</div><h1 className="mt-2 text-3xl font-black">PAINEL ADMINISTRATIVO</h1><p className="mt-2 text-sm text-white/45">Acesse o gerenciamento da loja.</p></div><input required type="email" autoComplete="email" placeholder="E-MAIL" value={email} onChange={e=>setEmail(e.target.value)} className="jg-input mt-8 w-full rounded px-4 py-3 text-sm"/><input required type="password" autoComplete="current-password" placeholder="SENHA" value={password} onChange={e=>setPassword(e.target.value)} className="jg-input mt-3 w-full rounded px-4 py-3 text-sm"/><button type="submit" disabled={loading} className="jg-button mt-5 w-full rounded px-4 py-3 text-sm disabled:cursor-not-allowed disabled:opacity-60">{loading?'ENTRANDO...':'ENTRAR'}</button>{error&&<p className="mt-4 rounded border border-red-500/20 bg-red-500/5 p-3 text-sm text-red-400">{error}</p>}</form></main>}
